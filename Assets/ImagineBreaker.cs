using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ImagineBreaker : MonoBehaviour
{
    private bool isMoving;
    public GameObject deletePoint;
  /*  private void Start()
    {
        deletePoint.SetActive(false);
        isMoving = false;
    }
    void Update()
    {
        if (!isMoving && OVRInput.Get(OVRInput.Button.PrimaryIndexTrigger))
        {
            StartMovement();
        }
        else if (isMoving && !OVRInput.Get(OVRInput.Button.PrimaryIndexTrigger))
        {
            EndMovement();
        }
        else if (isMoving && OVRInput.Get(OVRInput.Button.PrimaryIndexTrigger))
        {
            UpdateMovement();
        }
    }

    void StartMovement()
    {
        isMoving = true;
        deletePoint.SetActive(true);
    }

    void EndMovement()
    {
        isMoving = false;
        deletePoint.SetActive(false);
    }

    void UpdateMovement()
    {

    }*/
}
